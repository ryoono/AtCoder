#include <iostream>
#include <vector>
// #include <algorithm>
// #include <set>
// #include <map>
// #include <cstring>
#include <cmath>

using namespace std;

typedef long long ll;
typedef unsigned int ui;
typedef int si;

int main( void ){

    ui n, m;
    cin >> n >> m;

    ui buf;
    vector<pair<ui,ui>> mpoint;
    buf = sqrt(m);

    for( ui i=1; i<=buf; ++i ){
        for( ui j=i; j<=buf; ++j ){
            
        }
    }


    return 0;
}